# Redis production config (local/staging)
maxmemory 256mb
maxmemory-policy allkeys-lru
appendonly yes
appendfsync everysec
tcp-keepalive 300
timeout 0

# Security: bind handled by Docker network
protected-mode yes
