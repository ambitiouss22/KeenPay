"""Commerce: carts, orders, and the invariants money depends on.

Deliberately empty of re-exports - see ``modules/catalog/__init__.py``. A
convenience import here would be paid for with an import cycle between the
catalog service and the purchase flow.
"""
