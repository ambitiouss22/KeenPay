# KeenPay Frontend

Next.js split UI: chat (left) + live trace panel (right).

```bash
cd frontend && npm install && npm run dev
```

WebSocket client connects to `/ws/v1/session`. See `docs/API_SPEC.md` section 3.
