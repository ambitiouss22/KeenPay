[project]
name = "keenpay-worker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "redis>=5.2.0",
    "structlog>=24.4.0",
    "httpx>=0.28.0",
    "asyncpg>=0.30.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
